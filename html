<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover"/>
  <title>FahrzSTR | Galaxy Profile</title>

  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" rel="stylesheet">

  <style>
    :root {
      --bg-color: #050002;
      --card-bg: rgba(20, 10, 15, 0.65);
      --card-border: rgba(255, 20, 147, 0.2);
      --text-primary: #ffffff;
      --text-secondary: #e0e0e0;
      --text-tertiary: #a0a0a0;
      --accent-color: #FF1493;
      --accent-glow: #FF69B4;
      --button-bg: rgba(255, 255, 255, 0.08);
      --button-hover-bg: rgba(255, 20, 147, 0.2);
      --button-glow: 0 4px 20px rgba(255, 20, 147, 0.5);
      --shadow-3d: 0 25px 50px rgba(0,0,0,0.6);
    }

    [data-theme="light"] {
      --bg-color: #1a0f14;
      --card-bg: rgba(255, 240, 245, 0.75);
      --card-border: rgba(255, 20, 147, 0.15);
      --text-primary: #1a1a1a;
      --text-secondary: #4a4a4a;
      --text-tertiary: #7a7a7a;
      --accent-color: #FF1493;
      --accent-glow: #FF69B4;
      --button-bg: rgba(0, 0, 0, 0.05);
      --button-hover-bg: rgba(255, 20, 147, 0.1);
      --button-glow: 0 4px 15px rgba(255, 20, 147, 0.3);
      --shadow-3d: 0 15px 35px rgba(0,0,0,0.15);
    }

    * {
        box-sizing: border-box; margin: 0; padding: 0;
        -webkit-tap-highlight-color: transparent;
        -webkit-font-smoothing: antialiased;
    }
    html, body { height: 100%; overflow: hidden; }

    body {
      font-family: 'Poppins', sans-serif;
      background-color: var(--bg-color);
      color: var(--text-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      perspective: none;
      padding: 20px;
      padding-top: env(safe-area-inset-top);
      padding-bottom: env(safe-area-inset-bottom);
      transition: background-color 0.5s ease;
    }

    @media (min-width: 768px) { body { perspective: 1500px; } }

    #galaxy-canvas {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -2;
    }

    .nebula-overlay {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1;
      pointer-events: none; mix-blend-mode: screen;
      background: 
        radial-gradient(circle at 20% 30%, rgba(255, 20, 147, 0.18) 0%, transparent 55%),
        radial-gradient(circle at 80% 70%, rgba(255, 105, 180, 0.12) 0%, transparent 60%),
        radial-gradient(circle at 50% 50%, rgba(255, 255, 255, 0.05) 0%, transparent 65%);
      animation: nebulaPulse 12s infinite alternate ease-in-out;
    }

    @keyframes nebulaPulse {
      0% { opacity: 0.6; transform: scale(1); }
      100% { opacity: 1; transform: scale(1.05); }
    }

    .tilt-wrapper {
        display: flex; justify-content: center; align-items: center;
        width: 100%; height: 100%;
        transform-style: flat; padding: 10px;
    }
    @media (min-width: 768px) { .tilt-wrapper { transform-style: preserve-3d; } }

    .container {
      position: relative;
      background: var(--card-bg);
      backdrop-filter: blur(16px) saturate(120%);
      -webkit-backdrop-filter: blur(16px) saturate(120%);
      border: 1px solid var(--card-border);
      box-shadow: var(--shadow-3d), inset 0 0 30px rgba(255, 20, 147, 0.08);
      border-radius: 32px;
      padding: 35px 25px;
      width: 100%; max-width: 420px;
      text-align: center;
      will-change: transform, opacity;
      opacity: 0; transform: translateY(30px) scale(0.95);
      animation: proEntrance 0.8s cubic-bezier(0.22, 1, 0.36, 1) forwards;
      animation-delay: 0.2s;
      max-height: 88vh;
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
    }
    .container::-webkit-scrollbar { display: none; }

    @media (min-width: 768px) {
        .container {
            transition: transform 0.1s cubic-bezier(0.05, 0.79, 0.18, 0.96), box-shadow 0.3s ease, background-color 0.3s ease;
        }
         .tilt-wrapper:hover .container {
           box-shadow: 0 40px 80px rgba(0,0,0,0.6), 0 0 0 2px rgba(255, 20, 147, 0.4);
        }
    }

    @keyframes proEntrance {
      to { opacity: 1; transform: translateY(0) scale(1); }
    }

    .header-controls {
        display: flex; justify-content: space-between; align-items: center;
        margin-bottom: 20px; transform: none;
    }
    @media (min-width: 768px) { .header-controls { transform: translateZ(20px); } }

    .greeting { font-size: 14px; font-weight: 500; color: var(--text-tertiary); display: flex; align-items: center; gap: 8px; }
    .greeting-icon { color: var(--accent-color); font-size: 18px; }
    .control-btn {
      background: var(--button-bg); border: 1px solid var(--card-border);
      width: 38px; height: 38px; border-radius: 12px;
      display: flex; align-items: center; justify-content: center;
      color: var(--text-secondary); cursor: pointer; transition: all 0.2s ease;
    }
    .control-btn:active { background: var(--button-hover-bg); color: var(--accent-color); transform: scale(0.95); }

    .profile-wrapper { position: relative; width: 120px; height: 120px; margin: 0 auto 15px; transform: none; }
    @media (min-width: 768px) { .profile-wrapper { transform: translateZ(40px); } }
    video.profile-video {
      width: 100%; height: 100%; border-radius: 50%; object-fit: cover;
      border: 3px solid rgba(255,255,255,0.15);
      box-shadow: 0 0 0 4px var(--accent-color), 0 0 25px var(--accent-glow);
    }

    h1 { font-size: 26px; font-weight: 700; margin-bottom: 5px; transform: none; }
    @media (min-width: 768px) { h1 { transform: translateZ(30px); } }
    .role-badge {
        display: inline-block; font-size: 12px; font-weight: 600;
        color: var(--accent-color); 
        background: rgba(255, 20, 147, 0.15);
        padding: 4px 12px; border-radius: 20px; margin-bottom: 20px;
        border: 1px solid rgba(255, 20, 147, 0.3); transform: none;
    }
    @media (min-width: 768px) { .role-badge { transform: translateZ(25px); } }

    .typing-container {
      height: 28px; margin-bottom: 25px; display: flex; justify-content: center; align-items: center;
      font-family: 'JetBrains Mono', monospace; transform: none;
    }
    @media (min-width: 768px) { .typing-container { transform: translateZ(20px); } }
    .typing-text { font-size: 13px; color: var(--text-secondary); }
    .typing-text::before { content: '> '; color: var(--accent-color); font-weight: bold;}
    .typing-cursor {
        display: inline-block; width: 8px; height: 15px; background-color: var(--accent-color);
        margin-left: 5px; animation: blink 1s step-end infinite;
    }
    @keyframes blink { 50% { opacity: 0; } }

    .links-container { display: flex; flex-direction: column; gap: 10px; transform: none; margin-bottom: 30px; }
    @media (min-width: 768px) { .links-container { transform: translateZ(15px); } }

    .bio-link {
      display: flex; align-items: center; background: var(--button-bg); padding: 14px 18px;
      border-radius: 16px; color: var(--text-primary); text-decoration: none;
      border: 1px solid var(--card-border); position: relative; overflow: hidden;
      font-weight: 500; font-size: 15px; transition: all 0.2s ease;
      will-change: transform, background-color;
    }
    @media (hover: hover) and (min-width: 768px) {
        .bio-link:hover {
          background: var(--button-hover-bg); transform: translateY(-3px) scale(1.02) translateZ(10px);
          border-color: var(--accent-color); box-shadow: var(--button-glow);
        }
    }
    .bio-link:active { background: var(--button-hover-bg); transform: scale(0.98); border-color: var(--accent-color); }
    .bio-link .material-icons-round { margin-right: 12px; font-size: 22px; color: var(--accent-color); }
    .link-text { flex-grow: 1; text-align: left; }
    .link-arrow { font-size: 18px; color: var(--text-tertiary); opacity: 0.7; transform: none; transition: all 0.3s ease;}
    @media (hover: hover) and (min-width: 768px) { .link-arrow { opacity: 0.5; transform: translateX(0); } }

    .new-content-section {
        text-align: left;
        margin-bottom: 30px;
        padding-top: 20px;
        border-top: 1px solid var(--card-border);
        transform: none;
    }
    @media (min-width: 768px) { .new-content-section { transform: translateZ(10px); } }

    .section-title {
        font-size: 16px; font-weight: 600; margin-bottom: 15px; color: var(--text-primary);
        display: flex; align-items: center; gap: 8px;
    }
    .section-title .material-icons-round { color: var(--accent-color); font-size: 20px; }

    .skills-grid {
        display: flex; flex-wrap: wrap; gap: 8px;
    }
    .skill-tag {
        font-size: 12px; font-weight: 500; color: var(--text-primary);
        background: rgba(255, 255, 255, 0.05);
        padding: 6px 12px; border-radius: 20px;
        border: 1px solid var(--card-border);
        transition: all 0.3s ease;
    }
    .skill-tag:hover {
        background: rgba(255, 20, 147, 0.2);
        border-color: var(--accent-color);
        transform: translateY(-2px);
    }

    footer {
      margin-top: 10px; font-size: 11px; color: var(--text-tertiary);
      transform: none; font-family: 'JetBrains Mono', monospace;
      padding-bottom: 15px;
    }
    @media (min-width: 768px) { footer { transform: translateZ(10px); } }
  </style>
</head>

<body data-theme="dark">
  <canvas id="galaxy-canvas"></canvas>
  <div class="nebula-overlay"></div>

  <div class="tilt-wrapper" id="tiltBlock">
      <div class="container" id="mainCard">
        <div class="header-controls">
            <div class="greeting">
                <span class="material-icons-round greeting-icon" id="greet-icon">wb_sunny</span>
                <span id="greeting-text">Good Day</span>
            </div>
            <div style="display: flex; gap: 8px;">
                <button class="control-btn" onclick="toggleMusic()" aria-label="Music">
                    <span class="material-icons-round" id="music-icon">music_note</span>
                </button>
                <button class="control-btn" onclick="toggleTheme()" aria-label="Theme">
                    <span class="material-icons-round" id="theme-icon">dark_mode</span>
                </button>
            </div>
        </div>

        <div class="profile-wrapper">
            <video class="profile-video" autoplay muted loop playsinline preload="metadata" poster="https://via.placeholder.com/150/1a1a1a/FFFFFF?text=Profile">
                <source src="https://files.catbox.moe/7i0l2c.mp4" type="video/mp4">
            </video>
        </div>

        <h1>FahrzSTR</h1>
        <span class="role-badge">since 2024</span>

        <div class="typing-container">
            <span class="typing-text" id="typing"></span>
            <span class="typing-cursor"></span>
        </div>

        <div class="links-container">
            <a class="bio-link" href="https://wa.me/628882171625" target="_blank">
                <span class="material-icons-round">chat</span>
                <span class="link-text">WhatsApp</span>
                <span class="material-icons-round link-arrow">arrow_forward_ios</span>
            </a>
            <a class="bio-link" href="https://instagram.com/fahri.arrosid" target="_blank">
                <span class="material-icons-round">photo_camera</span>
                <span class="link-text">Instagram</span>
                 <span class="material-icons-round link-arrow">arrow_forward_ios</span>
            </a>
             <a class="bio-link" href="https://tiktok.com/@FahriNoWay" target="_blank">
                <span class="material-icons-round">movie</span>
                <span class="link-text">TikTok</span>
                 <span class="material-icons-round link-arrow">arrow_forward_ios</span>
            </a>
             <a class="bio-link" href="https://whatsapp.com/channel/0029Vb5qQZOFCCoOSzKbjx2w" target="_blank">
                <span class="material-icons-round">campaign</span>
                <span class="link-text">WhatsApp Channel</span>
                 <span class="material-icons-round link-arrow">arrow_forward_ios</span>
            </a>
            <a class="bio-link" href="https://wa.me/573163561940" target="_blank">
                <span class="material-icons-round">call</span>
                <span class="link-text">WhatsApp Reserves</span>
                 <span class="material-icons-round link-arrow">arrow_forward_ios</span>
            </a>
            <a class="bio-link" href="https://t.me/unknown" target="_blank">
                <span class="material-icons-round">send</span>
                <span class="link-text">Telegram</span>
                 <span class="material-icons-round link-arrow">arrow_forward_ios</span>
            </a>
        </div>

        <div class="new-content-section">
            <h3 class="section-title">
                <span class="material-icons-round">auto_awesome</span>
                Skills & Expertise
            </h3>
            <div class="skills-grid">
                <span class="skill-tag">Panel Management</span>
                <span class="skill-tag">Nokos Service</span>
                <span class="skill-tag">Fast Response</span>
                <span class="skill-tag">Trusted Seller</span>
                <span class="skill-tag">Digital Products</span>
            </div>
        </div>
        <footer>
            <code>© Fahrz STR 2025.</code>
        </footer>
      </div>
  </div>

  <audio id="bg-music" loop preload="metadata">
    <source src="https://files.catbox.moe/4jgs5q.mp3" type="audio/mpeg">
  </audio>

  <script>
    const canvas = document.getElementById('galaxy-canvas');
    const ctx = canvas.getContext('2d');
    let stars = [];
    const numStars = 450;
    let speed = 1.5;

    function setupCanvas() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        ctx.translate(canvas.width / 2, canvas.height / 2);
        stars = [];
        for (let i = 0; i < numStars; i++) {
            stars[i] = new Star();
        }
    }

    class Star {
        constructor() {
            this.reset();
            const colors = [
                'rgba(255, 255, 255, 0.9)',
                'rgba(255, 255, 255, 0.5)', 
                'rgba(255, 105, 180, 0.6)'
            ];
            this.color = colors[Math.floor(Math.random() * colors.length)];
        }
        reset() {
            this.x = (Math.random() * canvas.width - canvas.width / 2) * 2;
            this.y = (Math.random() * canvas.height - canvas.height / 2) * 2;
            this.z = Math.random() * canvas.width;
            this.pz = this.z;
        }
        update() {
            this.z = this.z - speed;
            if (this.z < 1) {
                this.reset();
                this.z = canvas.width;
                this.pz = this.z;
            }
        }
        draw() {
            let sx = this.x / this.z * canvas.width;
            let sy = this.y / this.z * canvas.height;
            let r = (canvas.width / this.z) * 0.7;

            ctx.fillStyle = this.color;
            ctx.beginPath();
            ctx.arc(sx, sy, r, 0, Math.PI * 2);
            ctx.fill();
        }
    }

    function animate() {
        ctx.fillStyle = getComputedStyle(document.documentElement).getPropertyValue('--bg-color') + '44';
        ctx.fillRect(-canvas.width / 2, -canvas.height / 2, canvas.width, canvas.height);

        for (let i = 0; i < stars.length; i++) {
            stars[i].update();
            stars[i].draw();
        }
        requestAnimationFrame(animate);
    }

    window.addEventListener('resize', setupCanvas);
    setupCanvas();
    animate();

    const tiltBlock = document.getElementById('tiltBlock');
    const card = document.getElementById('mainCard');
    const isDesktop = window.matchMedia("(min-width: 768px)").matches;
    
    if (isDesktop) {
        tiltBlock.addEventListener('mousemove', (e) => {
          const xAxis = (window.innerWidth / 2 - e.pageX) / 30;
          const yAxis = (window.innerHeight / 2 - e.pageY) / 30;
          card.style.transform = `rotateY(${xAxis}deg) rotateX(${yAxis}deg)`;
        });
        tiltBlock.addEventListener('mouseleave', () => {
          card.style.transform = `rotateY(0deg) rotateX(0deg)`;
          card.style.transition = 'transform 0.5s ease';
        });
        tiltBlock.addEventListener('mouseenter', () => {
           card.style.transition = 'transform 0.1s cubic-bezier(0.05, 0.79, 0.18, 0.96)';
        });
    }

    function updateGreeting() {
        const hour = new Date().getHours();
        const greetText = document.getElementById('greeting-text');
        const greetIcon = document.getElementById('greet-icon');
        if (hour >= 5 && hour < 11) { greetText.textContent = "Selamat Pagi"; greetIcon.textContent = "wb_twilight"; }
        else if (hour >= 11 && hour < 15) { greetText.textContent = "Selamat Siang"; greetIcon.textContent = "wb_sunny"; }
        else if (hour >= 15 && hour < 18) { greetText.textContent = "Selamat Sore"; greetIcon.textContent = "landscape"; }
        else { greetText.textContent = "Selamat Malam"; greetIcon.textContent = "nights_stay"; }
    }
    updateGreeting(); setInterval(updateGreeting, 60000);

    const audio = document.getElementById("bg-music");
    const musicBtn = document.querySelector(".control-btn[aria-label='Music']");
    const musicIcon = document.getElementById("music-icon");
    const themeIcon = document.getElementById("theme-icon");
    const body = document.body;
    let isDark = true;

    function toggleMusic() {
      if (audio.paused) {
        audio.play(); musicIcon.textContent = "pause";
        musicBtn.style.color = "var(--accent-color)";
      } else {
        audio.pause(); musicIcon.textContent = "music_note";
        musicBtn.style.color = "var(--text-secondary)";
      }
    }
    
    function toggleTheme() {
      isDark = !isDark;
      const next = isDark ? "dark" : "light";
      body.setAttribute("data-theme", next);
      themeIcon.textContent = isDark ? "dark_mode" : "light_mode";
    }

    const typingElement = document.getElementById("typing");
    const texts = [
      "Halo everyone, I'm FahrzSTR age: 16 years...",
      "Butuh panel?nokos? Langsung chat yaa~",
      "info selengkapnya follow my ch."
    ];
    let textIndex = 0; let charIndex = 0; let isDeleting = false; let typingSpeed = 80;
    
    function typeEffect() {
      const currentText = texts[textIndex];
      if (isDeleting) { charIndex--; typingSpeed = 30; } else { charIndex++; typingSpeed = 80; }
      typingElement.textContent = currentText.substring(0, charIndex);
      if (!isDeleting && charIndex === currentText.length) { isDeleting = true; typingSpeed = 2000; }
      else if (isDeleting && charIndex === 0) { isDeleting = false; textIndex = (textIndex + 1) % texts.length; typingSpeed = 500; }
      setTimeout(typeEffect, typingSpeed);
    }
    setTimeout(typeEffect, 1500);

    document.addEventListener("visibilitychange", () => { if (document.hidden && !audio.paused) toggleMusic(); });
  </script>
</body>
</html>
